# Architecture

## 概览

`<SYSTEM_OVERVIEW>`

## 架构目标

- `<ARCHITECTURE_GOAL_1>`
- `<ARCHITECTURE_GOAL_2>`

## 约束

- `<CONSTRAINT_1>`
- `<CONSTRAINT_2>`

## 系统上下文

描述用户、外部系统、信任边界和系统责任。

```text
<SYSTEM_CONTEXT_DIAGRAM>
```

## 容器与组件

| 组件 | 职责 | 输入 | 输出 | 依赖 |
| --- | --- | --- | --- | --- |
| `<COMPONENT>` | `<RESPONSIBILITY>` | `<INPUT>` | `<OUTPUT>` | `<DEPENDENCY>` |

## 数据流

1. `<DATA_FLOW_STEP_1>`
2. `<DATA_FLOW_STEP_2>`
3. `<DATA_FLOW_STEP_3>`

## 关键架构决策

| ADR | 决策 | 状态 | 原因 | 后果 |
| --- | --- | --- | --- | --- |
| ADR-001 | `<DECISION>` | Proposed | `<RATIONALE>` | `<CONSEQUENCES>` |

## 质量属性

- **性能**：`<PERFORMANCE_APPROACH>`
- **可靠性**：`<RELIABILITY_APPROACH>`
- **安全**：`<SECURITY_APPROACH>`
- **可扩展性**：`<SCALABILITY_APPROACH>`
- **可维护性**：`<MAINTAINABILITY_APPROACH>`
- **可观测性**：`<OBSERVABILITY_APPROACH>`

## 部署架构

```text
<DEPLOYMENT_DIAGRAM>
```

## 故障与恢复

| 故障场景 | 检测方式 | 降级策略 | 恢复方式 |
| --- | --- | --- | --- |
| `<FAILURE>` | `<DETECTION>` | `<FALLBACK>` | `<RECOVERY>` |

## 风险与待决事项

- `<OPEN_QUESTION_OR_RISK>`
